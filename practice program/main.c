#include<stdio.h>
int main(int argc, char const *argv[])
{
    int a,b,sum;
    printf("enter the value of a and b");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("the value of sum=%d",sum);
    return 0;
}
